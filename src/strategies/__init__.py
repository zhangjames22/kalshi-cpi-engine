"""Example strategies. Each strategy is a self-contained module that subclasses
`core.Strategy`. Strategies in this directory are the project's reference
examples — when adding a new one, follow the cpi_normal layout."""
